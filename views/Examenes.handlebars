<div class="py-4">
    <h2>Solicitud de Hora Imagenologia</h2>
    <hr class="w-50" />

<div class="py-4">
  <h2>Pedir Examenes</h2>
  <hr class="w-50" />
  <table class="table table-dark">
    <thead>
      <tr>
        <th scope="col">nro Examen</th>
        <th scope="col">id Examen</th>
        <th scope="col">Codigo Exámen</th>
        <th scope="col">Nombre Exámen</th>
        <th scope="col">Nombre Exámen</th>
        <th scope="col">Nombre Exámen</th>
      </tr>
    </thead>
   
    <tbody>      
      {{#each examenes}}
      <form method="post" action="/crear-examen" enctype="multipart/form-data">
        <tr>
          <td>{{this.id_hora}}</td>
          <td>{{this.id_examen}}</td>
          <td>{{this.fecha}}</td>
          <td><input type="checkbox" name="activa" value=""{{#if this.activa}}checked{{/if}}>{{this.activa}}</td>
          <td>{{this.codigo}}</td>
          <td>{{this.nombre_examen}}</td>
          <td>{{this.nombre}}</td>
        </tr> 
      {{/each}} 
      <input name="id_examen" type="text" value="1">  
      <input name="id_hora" type="text" value="10">          
        <div class="form-group row w-50 m-auto">
          <div class="form-group col-12 col-sm-12">
            <label for="fotos">Orden Medica</label>
            <input type="file" name="fotos" id="fotos" />
            <button class="btn btn-info mb-3" name="orden_medica" value="fotos">Tomar Hora</button>  
          </div>
        </div>
       </form> 
    </tbody>
  </table>
</div>

 